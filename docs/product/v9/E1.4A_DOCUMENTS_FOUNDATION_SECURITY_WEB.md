# E1.4A — Documents foundation/security/web

> Macro-PR A da E1.4. **Fechada tecnicamente em producao em 2026-08-22**
> apos `OWNER_MIGRATION_GATE_E14A`. Migration 073 aplicada uma vez via
> `apply_migration`, PR #444 mergeado e backend/web implantados. Sem Asaas, sem
> env/secrets e sem escrita de dado de negocio em producao.

## Escopo

- Documentos de frete continuam no bucket privado `fretes-documentos`; legado
  publico `comprovantes` segue intacto para fluxos antigos.
- Contrato v2 de upload: `document_contract_version=2`; documentos `outro`
  exigem `nome_documento` ou `descricao` no backend.
- Upload seguro para retry com `client_request_id` em documentos e evidencias ePOD.
- Auditoria minima para documento operacional: `uploaded` e `cancelled`, com
  ator, papel, timestamp, source, reason e metadata.
- Fundacao de multiplos recebedores/signatarios operacionais em
  `frete_documento_participantes`, separada de contratos SaaS.
- Web prioriza preview interno de PDF/imagem via signed URL curta; abrir fora,
  salvar e compartilhar ficam como acoes secundarias.

## Estado

- `E14A_IMPLEMENTED_IN_PR=true`
- `E14A_REQUIRES_MIGRATION=true`
- `E14A_MIGRATION=073_documents_foundation_security_web.sql`
- `E14A_MIGRATION_SHA256=7368bcd80009f1a21b42170d56d99f976dbca3a7aa0534ecb4d14c3f0e7dde91`
- `E14A_PRODUCTION_MIGRATION_APPLIED=true`
- `E14A_MIGRATION_TRACKING=20260822041647 073_documents_foundation_security_web`
- `E14A_PR=444`
- `E14A_PR_MERGE_SHA=1744d59bfd6e731452e85fbb01d3c2daa482a6a9`
- `E14A_DEPLOYED=true`
- `E14A_PRODUCTION_VALIDATED=true`
- `E14A_TECHNICAL_STATUS=CLOSED`
- `E14_OVERALL_STATUS=OPEN_E1.4B_PENDING`
- `A5_LEGACY_COMPROVANTES=PARTIAL_LEGACY_DEBT_PRESERVED`

## Fechamento em producao

- Precheck: PR #444 estava `OPEN/CLEAN/MERGEABLE`, HEAD autorizado
  `09021d68130e74d5bee85a0ed2948d4551449108`, CI completo verde
  (`backend=1640/1640`, `web=116/116`, `PG=145/145`, `PG_073=7/7`).
- Migration 073 aplicada uma vez pelo mecanismo canonico `apply_migration`.
  Tracking confirmado em `schema_migrations` como
  `20260822041647 073_documents_foundation_security_web`.
- Counts preservados, pois a 073 nao possui DML/backfill:
  `frete_documentos 16 -> 16`, `frete_epod_evidencias 10 -> 10`,
  `frete_ocorrencia_evidencias 0 -> 0`; novas tabelas
  `frete_documento_eventos` e `frete_documento_participantes` ficaram `0`.
- RLS ativo nas novas tabelas; policies tenant-aware criadas. Grants de
  `anon`/`authenticated` no schema publico continuam condicionados por RLS; o
  fluxo do produto segue backend-mediated.
- Buckets privados novos permanecem privados (`fretes-documentos`,
  `fretes-evidencias`, `fretes-odometro`); `comprovantes` permanece publico
  legado. Nenhum backfill, arquivo movido, upload ou alteracao de policy de
  storage foi feito.
- Backend Railway implantado automaticamente em
  `1744d59bfd6e731452e85fbb01d3c2daa482a6a9`
  (`deployment=96453b4b-5052-43bd-be22-ec1ab4afd078`, `SUCCESS`,
  `numReplicas=1`, healthcheck `/health` passou). Frontend GitHub Pages
  publicou no mesmo SHA.
- Smokes read-only: `/health` 200; endpoints protegidos de documentos e signed
  URL sem auth retornam 401; site 200. Logs/HTTP 5xx novos nos filtros criticos
  da E1.4A: 0.

## Fora de escopo

E1.4B mobile permanece fora desta entrega. Sem backfill. Sem remover ou migrar
`comprovantes`. Sem provider de assinatura externo. Sem nova cobranca, Asaas ou
alteracao comercial. `AUDIT_REPLACE`, `AUDIT_RETURN`, `AUDIT_ACK` e fluxos reais
de recebedores/signatarios ficam para frente (`FUTURE_B`).
